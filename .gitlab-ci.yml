# This file is generated by script 'ferrite/ci/gitlab/generate.py'

image: agerasev/debian-psc:0.3

variables:
  GIT_SUBMODULE_STRATEGY: "recursive"
  POETRY_VIRTUALENVS_IN_PROJECT: "true"

cache: &global_cache
  - key:
      files:
        - pyproject.toml
    paths:
      - poetry.lock
      - .venv/
  - key: cargo
    paths:
      - target/cargo

stages:
  - "-1"
  - "0"
  - "1"
  - "2"
  - "3"

yapf:
  stage: "-1"
  script:
    - poetry install
    - poetry run yapf --diff --recursive ferrite
  allow_failure: true

mypy:
  stage: "-1"
  script:
    - poetry install
    - poetry run mypy -p ferrite
  allow_failure: true

host.codegen.generate:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.codegen.generate
  artifacts:
    paths:
      - target/codegen

host.rustup.install:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.rustup.install
  artifacts:
    paths:
      - target/rustup
  cache:
    - *global_cache
    - key: "host.rustup.install:2556056a"
      paths:
        - target/rustup

arm.rustup.install:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture arm.rustup.install
  artifacts:
    paths:
      - target/rustup
  cache:
    - *global_cache
    - key: "arm.rustup.install:2556056a"
      paths:
        - target/rustup

aarch64.rustup.install:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture aarch64.rustup.install
  artifacts:
    paths:
      - target/rustup
  cache:
    - *global_cache
    - key: "aarch64.rustup.install:2556056a"
      paths:
        - target/rustup

host.codegen.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.codegen.build
  needs:
    - host.codegen.generate
  artifacts:
    paths:
      - target/codegen_test

host.app_test.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.app_test.build
  needs:
    - host.rustup.install
  artifacts:
    paths:
      - target/app

host.app_example.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.app_example.build
  needs:
    - host.rustup.install
  artifacts:
    paths:
      - target/app

arm.app_example.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture arm.app_example.build
  needs:
    - arm.rustup.install
  artifacts:
    paths:
      - target/app

aarch64.app_example.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture aarch64.app_example.build
  needs:
    - aarch64.rustup.install
  artifacts:
    paths:
      - target/app

host.codegen.test:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.codegen.test
  needs:
    - host.codegen.build

host.app_test.test:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.app_test.test
  needs:
    - host.app_test.build
    - host.rustup.install

host.app_example.run:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.app_example.run
  needs:
    - host.app_example.build
    - host.rustup.install

arm.all.build:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture arm.all.build
  needs:
    - arm.app_example.build

aarch64.all.build:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture aarch64.all.build
  needs:
    - aarch64.app_example.build

host.all.test:
  stage: "3"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.all.test
  needs:
    - host.app_test.test
    - host.codegen.test

host.all.run:
  stage: "3"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.all.run
  needs:
    - host.app_example.run
    - host.codegen.test
