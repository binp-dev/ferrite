# This file is generated by script 'ferrite/ci/gitlab/generate.py'

image: agerasev/debian-psc:0.3

variables:
  GIT_SUBMODULE_STRATEGY: "recursive"
  POETRY_VIRTUALENVS_IN_PROJECT: "true"

cache: &global_cache
  - key:
      files:
        - pyproject.toml
    paths:
      - poetry.lock
      - .venv/
  - key: cargo
    paths:
      - target/cargo

stages:
  - "-1"
  - "0"
  - "1"
  - "2"
  - "3"

yapf:
  stage: "-1"
  script:
    - poetry install
    - poetry run yapf --diff --recursive ferrite
  allow_failure: true

mypy:
  stage: "-1"
  script:
    - poetry install
    - poetry run mypy -p ferrite
  allow_failure: true

host.core_test.build:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.core_test.build
  artifacts:
    paths:
      - target/core_test

host.codegen.generate:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.codegen.generate
  artifacts:
    paths:
      - target/codegen

host.app_test.build:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.app_test.build
  artifacts:
    paths:
      - target/app_test

host.rustup.install:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.rustup.install
  artifacts:
    paths:
      - target/rustup
  cache:
    - *global_cache
    - key: "host.rustup.install:2556056a"
      paths:
        - target/rustup

arm.toolchain.download:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture arm.toolchain.download
  artifacts:
    paths:
      - target/toolchain_gcc-arm-10.2-2020.11-x86_64-arm-none-linux-gnueabihf
  cache:
    - *global_cache
    - key: "arm.toolchain.download:2d7f1739"
      paths:
        - target/toolchain_gcc-arm-10.2-2020.11-x86_64-arm-none-linux-gnueabihf

arm.rustup.install:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture arm.rustup.install
  artifacts:
    paths:
      - target/rustup
  cache:
    - *global_cache
    - key: "arm.rustup.install:2556056a"
      paths:
        - target/rustup

aarch64.toolchain.download:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture aarch64.toolchain.download
  artifacts:
    paths:
      - target/toolchain_gcc-arm-10.2-2020.11-x86_64-aarch64-none-linux-gnu
  cache:
    - *global_cache
    - key: "aarch64.toolchain.download:f93a1603"
      paths:
        - target/toolchain_gcc-arm-10.2-2020.11-x86_64-aarch64-none-linux-gnu

aarch64.rustup.install:
  stage: "0"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture aarch64.rustup.install
  artifacts:
    paths:
      - target/rustup
  cache:
    - *global_cache
    - key: "aarch64.rustup.install:2556056a"
      paths:
        - target/rustup

host.core_test.run:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.core_test.run
  needs:
    - host.core_test.build

host.codegen.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.codegen.build
  needs:
    - host.codegen.generate
  artifacts:
    paths:
      - target/codegen_test

host.app_test.run:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.app_test.run
  needs:
    - host.app_test.build

host.rust_test.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.rust_test.build
  needs:
    - host.rustup.install
  artifacts:
    paths:
      - target/rust_test

arm.app.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture arm.app.build
  needs:
    - arm.toolchain.download
  artifacts:
    paths:
      - target/app

arm.rust_test.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture arm.rust_test.build
  needs:
    - arm.rustup.install
  artifacts:
    paths:
      - target/rust_test

aarch64.app.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture aarch64.app.build
  needs:
    - aarch64.toolchain.download
  artifacts:
    paths:
      - target/app

aarch64.rust_test.build:
  stage: "1"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture aarch64.rust_test.build
  needs:
    - aarch64.rustup.install
  artifacts:
    paths:
      - target/rust_test

host.codegen.test:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.codegen.test
  needs:
    - host.codegen.build

host.rust_test.test:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.rust_test.test
  needs:
    - host.rust_test.build
    - host.rustup.install

arm.all.build:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture arm.all.build
  needs:
    - arm.app.build
    - arm.rust_test.build

aarch64.all.build:
  stage: "2"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture aarch64.all.build
  needs:
    - aarch64.app.build
    - aarch64.rust_test.build

host.all.test:
  stage: "3"
  script:
    - poetry install
    - poetry run python -u -m ferrite.manage --no-deps --no-capture host.all.test
  needs:
    - host.app_test.run
    - host.codegen.test
    - host.core_test.run
    - host.rust_test.test
